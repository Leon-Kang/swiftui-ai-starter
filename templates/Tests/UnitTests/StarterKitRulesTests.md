# Starter Kit Test Notes

- Validate single primary type rule through `scripts/check-project-conventions.sh`
- Validate registry sync through `scripts/check-component-registry.sh`
- Add project-specific unit tests after integrating the templates into a real app target
